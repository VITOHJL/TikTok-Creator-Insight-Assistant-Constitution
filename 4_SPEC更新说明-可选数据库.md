# SPEC更新说明：可选数据库模式

## 📋 更新日期
2025-01-27

## 🎯 更新目标

根据开发调试需求，更新SPEC以支持**可选数据库模式**：
- ✅ 开发环境默认启用SQLite
- ✅ 生产环境默认禁用（但可快速启用）
- ✅ 实现"插入即用，拔开也能正常运作"

## 📝 更新内容

### 1. 新增功能需求（FR-015 到 FR-023）

#### 核心需求
- **FR-015**: 系统必须支持可选的SQLite数据库用于开发调试
- **FR-016**: 系统在没有数据库的情况下也能完全正常工作
- **FR-017**: 通过环境变量控制数据库启用/禁用
- **FR-018**: 数据库启用时记录API请求和响应
- **FR-019**: 数据库禁用时正常处理所有用户请求

#### 实现要求
- **FR-020**: 数据库连接失败时，系统继续正常工作
- **FR-021**: 数据库操作使用try-catch包装，失败时静默处理
- **FR-022**: 数据库日志记录是异步操作，不阻塞API响应
- **FR-023**: 系统启动时检测数据库可用性

### 2. 更新Key Entities

新增实体：
- **API日志 (APILog)**: 代表一次API调用的记录（仅在数据库启用时使用）

### 3. 更新Assumptions

明确区分：
- ✅ **产品功能不需要持久化存储**：用户生成的内容不需要保存
- ✅ **开发调试可以使用数据库**：SQLite仅用于开发阶段的API日志记录

### 4. 更新Out of Scope

新增说明：
- **用户可见的历史记录保存和查看功能**不在MVP范围内
- 数据库（SQLite）的使用范围说明：
  - 开发环境：默认启用
  - 生产环境：默认禁用，但可快速启用
  - 可选组件：插入即用，拔开也能正常运作

### 5. 新增Architecture章节

详细说明**可选数据库模式**（Optional Database Pattern）：

#### 设计原则
1. **数据库是可选的，不是必需的**
2. **插入即用，拔开也能正常运作**
3. **开发环境默认启用，生产环境默认禁用**
4. **快速启用能力**

### 6. 更新Edge Cases

新增数据库相关边界情况：
- 数据库连接失败
- 数据库磁盘空间不足
- 数据库文件损坏
- 数据库启用/禁用切换

### 7. 更新Dependencies

明确区分：
- **必需依赖**：API密钥、网络连接、浏览器
- **可选依赖**：SQLite数据库（通过环境变量控制）

## 🎯 核心设计理念

### 可选数据库模式（Optional Database Pattern）

```
┌─────────────────────────────────────┐
│         用户请求                     │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     核心业务逻辑（不依赖数据库）      │
│  - 输入验证                          │
│  - API调用                           │
│  - 响应处理                          │
└──────────────┬──────────────────────┘
               │
               ├─────────────────┐
               │                 │
               ▼                 ▼
    ┌──────────────┐    ┌──────────────┐
    │  返回响应     │    │  数据库记录   │
    │  (必需)       │    │  (可选)       │
    └──────────────┘    └──────────────┘
                              │
                              │ (如果启用)
                              ▼
                    ┌─────────────────┐
                    │  SQLite数据库    │
                    │  (开发调试)      │
                    └─────────────────┘
```

### 环境变量控制

```bash
# 开发环境（默认启用）
NODE_ENV=development
ENABLE_DATABASE=true  # 默认值

# 生产环境（默认禁用）
NODE_ENV=production
ENABLE_DATABASE=false  # 默认值

# 生产环境快速启用（可选）
NODE_ENV=production
ENABLE_DATABASE=true   # 手动启用
```

## ✅ 验证要点

### 功能验证

1. **无数据库模式**：
   - ✅ 系统完全正常工作
   - ✅ 所有产品功能可用
   - ✅ API调用正常
   - ✅ 用户请求正常处理

2. **有数据库模式**：
   - ✅ API请求/响应被记录
   - ✅ 可以查询历史记录
   - ✅ 开发调试工具可用

3. **数据库故障处理**：
   - ✅ 数据库连接失败不影响功能
   - ✅ 数据库操作失败静默处理
   - ✅ 系统继续正常工作

### 测试场景

1. **场景1：开发环境（数据库启用）**
   ```
   设置：NODE_ENV=development, ENABLE_DATABASE=true
   预期：API调用被记录到数据库，可以查询历史
   ```

2. **场景2：生产环境（数据库禁用）**
   ```
   设置：NODE_ENV=production, ENABLE_DATABASE=false
   预期：系统正常工作，不记录数据库
   ```

3. **场景3：数据库连接失败**
   ```
   设置：ENABLE_DATABASE=true，但数据库文件不可写
   预期：系统正常工作，数据库操作失败但不影响功能
   ```

4. **场景4：生产环境快速启用**
   ```
   设置：NODE_ENV=production, ENABLE_DATABASE=true
   预期：数据库自动创建，开始记录日志
   ```

## 📊 更新统计

- **新增功能需求**：9个（FR-015到FR-023）
- **新增实体**：1个（APILog）
- **新增边界情况**：4个
- **新增架构章节**：1个（Optional Database Pattern）
- **更新章节**：Assumptions, Out of Scope, Dependencies, Edge Cases

## 🎯 下一步

SPEC已更新完成，现在可以：
1. ✅ 继续创建实现计划（`/speckit.plan`）
2. ✅ 生成任务清单（`/speckit.tasks`）
3. ✅ 开始实现（`/speckit.implement`）

所有更新都符合：
- ✅ MVP原则（产品功能不需要数据库）
- ✅ 开发效率（开发阶段可以使用数据库）
- ✅ 灵活性（生产环境可快速启用）

