# SPEC更新说明：三阶段协作式创作流程

**日期**: 2025-01-05  
**更新类型**: 核心设计变更  
**影响范围**: 用户故事、功能需求、数据模型、API规范

## 更新背景

基于用户反馈和产品迭代需求，将原有的"一次性生成"模式升级为"三阶段协作式创作流程"，以提升脚本生成质量和用户参与度。

## 核心变更

### 1. 设计理念转变

**原设计**：
- 用户输入 → 一次性生成所有内容（脚本、Hashtag、音乐）
- 用户被动接收结果
- 无法迭代优化

**新设计**：
- 三阶段协作式流程
- 每个阶段都支持迭代优化
- 支持回退到上一阶段
- 会话状态持久化

### 2. 三阶段流程

#### 阶段1：金句选择（Hook Selection）
- **目标**：找到能引起共鸣的"金句/钩子"
- **生成数量**：3-5个金句选项
- **用户操作**：选择、替换、微调、手动编辑后优化
- **确认机制**：用户确认后进入阶段2

#### 阶段2：内容创作（Content Development）
- **目标**：围绕金句展开丰富的内容
- **生成数量**：3个详细的内容要点（不是简单大纲，而是丰富的内容描述）
- **用户操作**：编辑、优化、基于要点展开
- **确认机制**：用户确认后进入阶段3

#### 阶段3：脚本转换（Script Conversion）
- **目标**：将内容转换为可执行的短视频脚本
- **生成数量**：3种不同风格的脚本（故事叙述型、教程教学型、对比评测型）
- **专业元素**：情绪锚点、信息密度控制、反转/冲突设计、记忆点植入
- **用户操作**：查看、优化单个脚本、一键复制

### 3. 新增功能需求

添加了 FR-028 至 FR-050，涵盖：
- 阶段1功能需求（FR-028 - FR-033）
- 阶段2功能需求（FR-034 - FR-040）
- 阶段3功能需求（FR-041 - FR-046）
- 会话管理与状态持久化（FR-047 - FR-050）

### 4. 数据模型更新

#### 新增实体

1. **CreationSession（创作会话）**
   - 会话ID、当前阶段、用户输入
   - 选中的金句、内容大纲、生成的脚本
   - 交互历史、创建/更新时间

2. **Hook（金句）**
   - 金句文本、是否选中
   - 关联会话ID

3. **ContentPoint（内容要点）**
   - 标题、详细内容（不是简单大纲）
   - 排序、是否展开

#### 更新实体

**ScriptOutline（脚本大纲）**
- 新增字段：`emotionalAnchors`（情绪锚点）
- 新增字段：`memoryPoints`（记忆点）
- 新增字段：`conflictDesign`（冲突设计）
- 新增字段：`informationDensity`（信息密度）
- 新增字段：`sessionId`（关联会话）

### 5. 数据库Schema更新

新增表：`creation_sessions`
- 存储会话状态
- 支持会话恢复
- 支持阶段回退

### 6. API规范更新

创建了新的API规范文档：`contracts/three-stage-api.md`

**新增API端点**（14个）：
1. `POST /api/session/create` - 创建会话
2. `POST /api/session/{id}/stage1/generate-hooks` - 生成金句
3. `POST /api/session/{id}/stage1/replace-hooks` - 替换金句
4. `POST /api/session/{id}/stage1/refine-hook` - 微调金句
5. `POST /api/session/{id}/stage1/select-hook` - 选择金句
6. `POST /api/session/{id}/stage2/generate-content` - 生成内容
7. `PUT /api/session/{id}/stage2/content-point/{pointId}` - 更新内容要点
8. `POST /api/session/{id}/stage2/optimize-content` - 优化内容
9. `POST /api/session/{id}/stage2/expand-point` - 展开要点
10. `POST /api/session/{id}/stage2/confirm-content` - 确认内容
11. `POST /api/session/{id}/stage3/generate-scripts` - 生成脚本
12. `POST /api/session/{id}/stage3/refine-script` - 微调脚本
13. `GET /api/session/{id}` - 获取会话状态
14. `POST /api/session/{id}/go-back` - 返回上一阶段

### 7. 用户故事更新

更新了 **User Story 2**，从"一次性生成脚本"改为"三阶段协作式创作流程"。

新增了详细的验收场景：
- 阶段1的5个验收场景
- 阶段2的5个验收场景
- 阶段3的4个验收场景
- 会话管理的3个验收场景

## 关键设计决策

### 1. 会话持久化
- **决策**：支持会话持久化，允许刷新后恢复
- **原因**：三阶段流程较长，用户可能中途离开
- **实现**：数据库存储会话状态

### 2. 阶段回退
- **决策**：允许用户回退到上一阶段修改
- **原因**：提升用户体验，允许用户重新选择
- **实现**：通过 `go-back` API端点实现

### 3. 内容要点详细度
- **决策**：阶段2的内容要点要详细丰富，不是简单大纲
- **原因**：用户需要看到具体内容，才能判断是否满意
- **实现**：Prompt设计强调"详细描述，包含具体场景、情感、细节"

### 4. 金句数量
- **决策**：生成3-5个金句选项
- **原因**：给用户足够选择空间，但不过多造成选择困难
- **实现**：API生成3-5个选项

### 5. 脚本专业元素
- **决策**：脚本必须包含情绪锚点、信息密度、冲突设计、记忆点等专业元素
- **原因**：提升脚本质量，体现"短视频拆解大师"的专业性
- **实现**：Prompt设计强调这些元素，输出结构化数据

## 影响评估

### 需要更新的文件
- ✅ `spec.md` - 已更新（用户故事、功能需求）
- ✅ `data-model.md` - 已更新（新增实体、更新实体）
- ✅ `contracts/three-stage-api.md` - 已创建（新API规范）
- ⏸️ `tasks.md` - 待更新（需要添加新的实现任务）
- ⏸️ `plan.md` - 待更新（需要更新实现计划）

### 向后兼容性
- **旧API**：`POST /api/generate` 保留，但标记为 legacy
- **新API**：三阶段API为新的标准流程
- **迁移策略**：可以同时支持两种模式，逐步迁移

## 下一步行动

1. **更新实现计划** (`plan.md`)
   - 定义三阶段实现的技术方案
   - 确定开发优先级

2. **更新任务清单** (`tasks.md`)
   - 添加三阶段交互的实现任务
   - 添加会话管理的实现任务

3. **开始代码实现**
   - 先实现会话管理基础
   - 再实现阶段1（金句选择）
   - 然后实现阶段2（内容创作）
   - 最后实现阶段3（脚本转换）

## 注意事项

1. **数据库迁移**：需要创建新的 `creation_sessions` 表
2. **前端重构**：需要重新设计UI，支持三阶段流程
3. **状态管理**：前端需要管理复杂的会话状态
4. **Prompt优化**：每个阶段都需要精心设计的Prompt
5. **测试策略**：需要测试三阶段流程的完整性和回退功能

## 总结

这次更新是一次重大的产品设计变更，从"工具型"转向"协作型"，核心目标是提升脚本生成质量和用户参与度。虽然增加了开发复杂度，但预期能显著提升产品价值和用户满意度。

